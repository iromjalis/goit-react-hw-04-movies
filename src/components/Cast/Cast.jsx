import React from 'react';
import PropTypes from 'prop-types';
//import { Test } from './Cast.styles';

const Cast = props => <div className="CastWrapper">Cast content</div>;

Cast.propTypes = {
  // bla: PropTypes.string,
};

Cast.defaultProps = {
  // bla: 'test',
};

export default Cast;
